import {
	BrainIcon,
	BubblesIcon,
	CannabisIcon,
	CloudRainIcon,
	LeafIcon,
	RocketIcon,
	SnowflakeIcon,
} from "lucide-react";

import {
	createAutumnParticle,
	createBubbleParticle,
	createMatrixParticle,
	createRainParticle,
	createSakuraParticle,
	createSnowParticle,
	createSpaceParticle,
	renderAutumnBackground,
	renderAutumnParticle,
	renderBubbleBackground,
	renderBubbleParticle,
	renderMatrixBackground,
	renderMatrixParticle,
	renderRainBackground,
	renderRainParticle,
	renderSakuraBackground,
	renderSakuraParticle,
	renderSnowBackground,
	renderSnowParticle,
	renderSpaceBackground,
	renderSpaceParticle,
	updateAutumnParticle,
	updateBubbleParticle,
	updateMatrixParticle,
	updateRainParticle,
	updateSakuraParticle,
	updateSnowParticle,
	updateSpaceParticle,
} from "@vista-voyage/theme";

import type { Particle, Size, Theme, ThemeType } from "@vista-voyage/type";

export const THEMES: Record<ThemeType, Theme> = {
	autumn: {
		icon: CannabisIcon,
		label: "紅葉",
		particleCount: 50,
		createParticle: createAutumnParticle,
		renderBackground: renderAutumnBackground,
		renderParticle: renderAutumnParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateAutumnParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	bubbles: {
		icon: BubblesIcon,
		label: "水中",
		particleCount: 60,
		createParticle: createBubbleParticle,
		renderBackground: renderBubbleBackground,
		renderParticle: renderBubbleParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateBubbleParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	matrix: {
		icon: BrainIcon,
		label: "マトリックス",
		particleCount: 40,
		createParticle: createMatrixParticle,
		renderBackground: renderMatrixBackground,
		renderParticle: renderMatrixParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateMatrixParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	rain: {
		icon: CloudRainIcon,
		label: "雨",
		particleCount: 150,
		createParticle: createRainParticle,
		renderBackground: renderRainBackground,
		renderParticle: renderRainParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateRainParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	sakura: {
		icon: LeafIcon,
		label: "桜",
		particleCount: 60,
		createParticle: createSakuraParticle,
		renderBackground: renderSakuraBackground,
		renderParticle: renderSakuraParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateSakuraParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	snow: {
		icon: SnowflakeIcon,
		label: "雪",
		particleCount: 150,
		createParticle: createSnowParticle,
		renderBackground: renderSnowBackground,
		renderParticle: renderSnowParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateSnowParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
	space: {
		icon: RocketIcon,
		label: "宇宙",
		particleCount: 300,
		createParticle: createSpaceParticle,
		renderBackground: renderSpaceBackground,
		renderParticle: renderSpaceParticle as (
			ctx: CanvasRenderingContext2D,
			particle: Particle,
		) => void,
		updateParticle: updateSpaceParticle as (
			p: Particle,
			index: number,
			size: Size,
			particles: Particle[],
			createParticle: () => Particle,
		) => void,
	},
};
